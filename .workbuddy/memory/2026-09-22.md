# 2026-09-22 工作日志

## 前两场（上午，已提交）

- 新建分支 **`ddmar`**（从 `video` 切出），工作区干净。
- `12:13` `beab38d` *oddmar: continue offline video startup fixes*（34 文件）：
  两份文档、`javastubs/android*.{h,cpp}`（含 **`getObbDir`/`getObbDirs` 实现**）、`bd_assetlocator.cpp`、
  `javac.cpp`、`libjnivm`（`method.cpp`/`vm.cpp`/`findclass.cpp`）、`thunks/ndk/media*`+`ndk.cpp`、
  `libc/fcntl.cpp`+`misc.cpp`+`stdio.cpp`、`platform/common/debug_utils.*`、
  `projects/unityloader/main.cpp`+`javastubs/*`、`scripts/container-run-game-seq.sh`。
- `12:55` `eb68cf7` *oddmar: add opt-in Unity video translation bypass*：
  `projects/unityloader/main.cpp`(+37) + `thunks/libc/fcntl.cpp`(+27/−3)。
  两个绕过点：① `clean_jar_path()` 增加 `<cwd>/gamedata/assets` 映射；
  ② `BD_BYPASS_VIDEO_TRANSLATE=1` hook `libunity.so + 0x4c124c`。

## 第三场（下午，本会话；只读 + 跑测 + 文档，未改代码）

### 做了什么

1. 读 `docs/HANDOFF-ODDMAR.md`，核对分支/提交/工作区（`ddmar`，clean）。
2. **反汇编 `libunity.so`**（15 287 168 B，stripped，BuildID sha1 `664018de51c6…`），
   把 `0x4c124c` 的性质钉死：**函数入口，但是一个虚调用转发 thunk**
   （`mov x10,x0` / `ldr x0,[x10,#0x410]` / `ldr x5,[x11,#312]` / `br x5`），
   4 个调用点 `0x53f150`、`0x5452dc`、`0x78b81c`、`0x78d534`。
3. 读调用点 `0x53f138-0x53f164` 得出真实契约：`bool f(ctx, in_path, out_ptr, out_len)`，
   **返回 bool（`tbz w0,#0`）**，结果写 **a2/a3 两个相邻 8 字节输出槽**，**没有 sret**。
4. 跑了 3 轮（`BOOT_LOADER=build-oddmar-verbose`，含 hook 与参数日志）：
   - args1/args2：**哨兵 `unity.toml`** → 两次完全一致地早死（1 357/1 358 行、`init time`=0、
     `Invalid Reference, Unexpected Type`×2）。
   - args3：正常 toml + `BD_BYPASS_VIDEO_TRANSLATE=1` → 79 168 行、`init time`=1、hook 触发 1 次，
     **拿到 8 个入参真值**（`a0-a2=0x38`、`a1-a2=0x470`、`a3-a2=8` 与调用栈偏移逐项吻合）。
5. 留档哨兵配置为 `/game/Oddmar/unity.toml.sentinel-0922`，**还原正常配置**
   （`unity.toml.bak-rd9`）。
6. 更新 `docs/HANDOFF-ODDMAR.md`（新增 §0.6，改写 §⛔0-1 / §0 / §7.0 / §8 / §9.2-9.3）
   与 `docs/CASE_STUDIES.md`（新增「案例四（续三）：hook 了一个转发 thunk」）。

### 结论（写进文档的核心）

- **hook 现在必然失败的原因有两条**：① 返回 `char*`（对齐 → `w0.bit0=0`）而调用方用
  `tbz w0,#0` 判 bool；② a2/a3 两个输出槽从未写。⇒ 正确修法：写槽 + `return 1`。
- **`§0.5-C` 的因果链被证伪**：`getObbDir` 已实现并返回 `/game/Oddmar/gamedata`，
  URL 依旧 hostless `jar:file://!/assets/...` ⇒ host 不是来自 obb。
- **`Invalid Reference, Unexpected Type` 与 `JNIVM_ENABLE_RETURN_NON_ZERO` 无关**，
  是**哨兵 toml** 的连带症状（弹错误对话框 → JBRIDGE 代理类型不匹配）。
- **"已加入的参数日志"上一场一次都没打出来**（历史日志 `grep -c` = 0），那两行是最后一次运行之后才编进去的。

### 容器内新增产物（留档）

`log-args1/2/3.txt`、`args1/2/3.out`、`seq-args1/2/3/`、`unity.toml.sentinel-0922`。
`build-oddmar-verbose/unityloader`（容器 05:12）才含 hook 与参数日志；`build-regress` 两个字符串都是 0。

## 下一场应从哪开始

`docs/HANDOFF-ODDMAR.md` §7.0 第 0 组：把 **`BD_BYPASS_VIDEO_TRANSLATE` 的 hook 改成真实 ABI**
（写 a2/a3、`return 1`），验收三条：不再出现 `could not translate`、日志出现对
`gamedata/assets/Videos/….mp4` 的 `stat`/`open`、`-10004` 消失。

## 第三场（追加）：文档合并 + 一次「整个 docs 目录被删」的事故

### 做了什么（用户要求：Oddmar 部分并入 HANDOFF，删除 CASE_STUDIES.md）

1. 核对：`docs/CASE_STUDIES.md` 447 行 / 6 节，其中**只有 3 节属 Oddmar**（案例四、四·续、四·续三）；
   其余 Skul / Maximus2 / FiveHearts 非 Oddmar。用户明确选择**照字面直接删**（其余三案丢弃）。
2. 用一次性脚本 `%TEMP%\bogodroid_merge_cs.py` 把 Oddmar 三节（原 128–447 行 = 318 行）
   并入 `docs/HANDOFF-ODDMAR.md`，落为 **§10 案例 A / §11 案例 B / §12 案例 C**，
   原「§10 相关文档」顺延为 **§13**；块首加「案例存档说明」。文档 1170 → **1504 行**。
3. 引用全部改指：`AGENTS.md`(2 处)、`docs/PORTING_PLAYBOOK.md`(3 处)、`docs/FIVEHEARTS.md`(3 处)，
   以及 HANDOFF 内部 4 处（L414 / L621 / L895 / 相关文档条目）+ §10→§13 版本行交叉引用。
   校验：三份外部文档 `grep -c CASE_STUDIES` **全为 0**。
4. `git rm -f docs/CASE_STUDIES.md`（因带未提交改动，删前先备份到
   `%TEMP%\CASE_STUDIES.md.deleted-20260922`，25952 B）。

### ⚠️ 事故：整个 `docs/` 目录被移进回收站（15:11）

- `D:\Locke\gitee\Bogodroid-anbernic\docs\` **整个目录**被删入回收站（不是 git 操作）：
  回收站里能查到 `$IL1UUHQ` → `…\Bogodroid-anbernic\docs`、`$IF5G7MG.md` → `…\docs\CASE_STUDIES.md`。
  当时 `git status` 把 4 份 md 全报成工作树删除，而合并结果**尚未提交**，一度以为全丢。
- 已从 `D:\$Recycle.Bin\S-1-5-21-655826624-3319794-2875605929-500\$RL1UUHQ\` 恢复 4 个文件，
  **零丢失**：HANDOFF 96657 B/1504 行、FIVEHEARTS 25590 B/458 行、PORTING_PLAYBOOK 8754 B/140 行、
  H700 2039 B。恢复用 `cp -p` 保留 mtime。
- 教训：**目录级删除只有回收站 / IDE 本地历史能救**（git 里没有未提交内容）；
  合并、改写这类"只在工作树里的成果"，做完就尽快提交，别攒着。
