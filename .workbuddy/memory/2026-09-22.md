# 2026-09-22 工作日志

## 前两场（上午，已提交）

- 新建分支 **`ddmar`**（从 `video` 切出），工作区干净。
- `12:13` `beab38d` *oddmar: continue offline video startup fixes*（34 文件）：
  两份文档、`javastubs/android*.{h,cpp}`（含 **`getObbDir`/`getObbDirs` 实现**）、`bd_assetlocator.cpp`、
  `javac.cpp`、`libjnivm`（`method.cpp`/`vm.cpp`/`findclass.cpp`）、`thunks/ndk/media*`+`ndk.cpp`、
  `libc/fcntl.cpp`+`misc.cpp`+`stdio.cpp`、`platform/common/debug_utils.*`、
  `projects/unityloader/main.cpp`+`javastubs/*`、`scripts/container-run-game-seq.sh`。
- `12:55` `eb68cf7` *oddmar: add opt-in Unity video translation bypass*：
  `projects/unityloader/main.cpp`(+37) + `thunks/libc/fcntl.cpp`(+27/−3)。
  两个绕过点：① `clean_jar_path()` 增加 `<cwd>/gamedata/assets` 映射；
  ② `BD_BYPASS_VIDEO_TRANSLATE=1` hook `libunity.so + 0x4c124c`。

## 第三场（下午，本会话；只读 + 跑测 + 文档，未改代码）

### 做了什么

1. 读 `docs/HANDOFF-ODDMAR.md`，核对分支/提交/工作区（`ddmar`，clean）。
2. **反汇编 `libunity.so`**（15 287 168 B，stripped，BuildID sha1 `664018de51c6…`），
   把 `0x4c124c` 的性质钉死：**函数入口，但是一个虚调用转发 thunk**
   （`mov x10,x0` / `ldr x0,[x10,#0x410]` / `ldr x5,[x11,#312]` / `br x5`），
   4 个调用点 `0x53f150`、`0x5452dc`、`0x78b81c`、`0x78d534`。
3. 读调用点 `0x53f138-0x53f164` 得出真实契约：`bool f(ctx, in_path, out_ptr, out_len)`，
   **返回 bool（`tbz w0,#0`）**，结果写 **a2/a3 两个相邻 8 字节输出槽**，**没有 sret**。
4. 跑了 3 轮（`BOOT_LOADER=build-oddmar-verbose`，含 hook 与参数日志）：
   - args1/args2：**哨兵 `unity.toml`** → 两次完全一致地早死（1 357/1 358 行、`init time`=0、
     `Invalid Reference, Unexpected Type`×2）。
   - args3：正常 toml + `BD_BYPASS_VIDEO_TRANSLATE=1` → 79 168 行、`init time`=1、hook 触发 1 次，
     **拿到 8 个入参真值**（`a0-a2=0x38`、`a1-a2=0x470`、`a3-a2=8` 与调用栈偏移逐项吻合）。
5. 留档哨兵配置为 `/game/Oddmar/unity.toml.sentinel-0922`，**还原正常配置**
   （`unity.toml.bak-rd9`）。
6. 更新 `docs/HANDOFF-ODDMAR.md`（新增 §0.6，改写 §⛔0-1 / §0 / §7.0 / §8 / §9.2-9.3）
   与 `docs/CASE_STUDIES.md`（新增「案例四（续三）：hook 了一个转发 thunk」）。

### 结论（写进文档的核心）

- **hook 现在必然失败的原因有两条**：① 返回 `char*`（对齐 → `w0.bit0=0`）而调用方用
  `tbz w0,#0` 判 bool；② a2/a3 两个输出槽从未写。⇒ 正确修法：写槽 + `return 1`。
- **`§0.5-C` 的因果链被证伪**：`getObbDir` 已实现并返回 `/game/Oddmar/gamedata`，
  URL 依旧 hostless `jar:file://!/assets/...` ⇒ host 不是来自 obb。
- **`Invalid Reference, Unexpected Type` 与 `JNIVM_ENABLE_RETURN_NON_ZERO` 无关**，
  是**哨兵 toml** 的连带症状（弹错误对话框 → JBRIDGE 代理类型不匹配）。
- **"已加入的参数日志"上一场一次都没打出来**（历史日志 `grep -c` = 0），那两行是最后一次运行之后才编进去的。

### 容器内新增产物（留档）

`log-args1/2/3.txt`、`args1/2/3.out`、`seq-args1/2/3/`、`unity.toml.sentinel-0922`。
`build-oddmar-verbose/unityloader`（容器 05:12）才含 hook 与参数日志；`build-regress` 两个字符串都是 0。

## 下一场应从哪开始

`docs/HANDOFF-ODDMAR.md` §7.0 第 0 组：把 **`BD_BYPASS_VIDEO_TRANSLATE` 的 hook 改成真实 ABI**
（写 a2/a3、`return 1`），验收三条：不再出现 `could not translate`、日志出现对
`gamedata/assets/Videos/….mp4` 的 `stat`/`open`、`-10004` 消失。

## 第三场（追加）：文档合并 + 一次「整个 docs 目录被删」的事故

### 做了什么（用户要求：Oddmar 部分并入 HANDOFF，删除 CASE_STUDIES.md）

1. 核对：`docs/CASE_STUDIES.md` 447 行 / 6 节，其中**只有 3 节属 Oddmar**（案例四、四·续、四·续三）；
   其余 Skul / Maximus2 / FiveHearts 非 Oddmar。用户明确选择**照字面直接删**（其余三案丢弃）。
2. 用一次性脚本 `%TEMP%\bogodroid_merge_cs.py` 把 Oddmar 三节（原 128–447 行 = 318 行）
   并入 `docs/HANDOFF-ODDMAR.md`，落为 **§10 案例 A / §11 案例 B / §12 案例 C**，
   原「§10 相关文档」顺延为 **§13**；块首加「案例存档说明」。文档 1170 → **1504 行**。
3. 引用全部改指：`AGENTS.md`(2 处)、`docs/PORTING_PLAYBOOK.md`(3 处)、`docs/FIVEHEARTS.md`(3 处)，
   以及 HANDOFF 内部 4 处（L414 / L621 / L895 / 相关文档条目）+ §10→§13 版本行交叉引用。
   校验：三份外部文档 `grep -c CASE_STUDIES` **全为 0**。
4. `git rm -f docs/CASE_STUDIES.md`（因带未提交改动，删前先备份到
   `%TEMP%\CASE_STUDIES.md.deleted-20260922`，25952 B）。

### ⚠️ 事故：整个 `docs/` 目录被移进回收站（15:11）

- `D:\Locke\gitee\Bogodroid-anbernic\docs\` **整个目录**被删入回收站（不是 git 操作）：
  回收站里能查到 `$IL1UUHQ` → `…\Bogodroid-anbernic\docs`、`$IF5G7MG.md` → `…\docs\CASE_STUDIES.md`。
  当时 `git status` 把 4 份 md 全报成工作树删除，而合并结果**尚未提交**，一度以为全丢。
- 已从 `D:\$Recycle.Bin\S-1-5-21-655826624-3319794-2875605929-500\$RL1UUHQ\` 恢复 4 个文件，
  **零丢失**：HANDOFF 96657 B/1504 行、FIVEHEARTS 25590 B/458 行、PORTING_PLAYBOOK 8754 B/140 行、
  H700 2039 B。恢复用 `cp -p` 保留 mtime。
- 教训：**目录级删除只有回收站 / IDE 本地历史能救**（git 里没有未提交内容）；
  合并、改写这类"只在工作树里的成果"，做完就尽快提交，别攒着。

## 第三场（收尾）：提交 / 推送 + 分支改名 + 新 worktree

### 完成的事

1. 提交 **`7406cae`** *docs: fold Oddmar case studies into handoff, drop CASE_STUDIES*
   （7 文件，+780/−441，含 `docs/CASE_STUDIES.md` 的删除）。
2. 分支改名 **`ddmar` → `oddmar`** 并推送：`origin/oddmar` = 7406cae，跟踪已设置。
   ⚠️ **`origin/ddmar` 还在**（eb68cf7），本轮没删。
3. 新建 worktree **`D:\Locke\gitee\Bogodroid-oddmar`** ← 分支 `oddmar`；
   子模块已 `submodule update --init --recursive`（`tomlplusplus` v3.4.0-4-g89406c7）。
   **`D:\Locke\gitee\Bogodroid-anbernic` 已切回 `video`**（058ddca）。

   | 目录 | 分支 |
   |------|------|
   | `D:\Locke\gitee\Bogodroid` | `unity6` |
   | `D:\Locke\gitee\Bogodroid-anbernic` | `video` |
   | `D:\Locke\gitee\Bogodroid-oddmar` | **`oddmar`** |

   > `git worktree add` 默认拒绝同一分支被两个工作树检出（`fatal: 'oddmar' is already
   > used by worktree at ...`），所以必须先把本目录让出去。别用 `--force`。
   > **oddmar 的活以后在 `D:\Locke\gitee\Bogodroid-oddmar` 里干。**

### ⚠️ 又一次"目录被删"事故（15:39，比 15:11 那次更广）

- 切到 `video` 的瞬间，**28 个跟踪文件从磁盘消失**：`scripts/` 整目录、`javastubs/` 部分文件、
  `docs/H700_VIDEO_DECODE_SPIKE.md`、`thunks/ndk/media.cpp`、`libjnivm` 若干、`AGENTS.md`、`main.cpp`…
- 回收站里对应条目时间戳 **15:39**，而且是**oddmar 与 video 有差异的那些路径**
  （连 `.workbuddy/` 整目录、`AUTO_MERGE.lock`、`index.lock` 都进去了）。
  ⇒ 判读：有外部工具/IDE 在按分支差异做"**删了再写**"，且**只删没写回**。
- 处置：`git checkout -- .` 全量恢复，**零丢失**，28 条 status 归零；
  `.workbuddy/memory/` 因 video 分支不跟踪该目录，已从本 worktree 拷回 anbernic。
- **规矩**：在 anbernic 目录里做分支切换前，先确认无未提交改动；切完立刻 `git status` 复核。

### 📄 docs/HANDOFF-ODDMAR.md 精简（本次工作）

- **1505 行 / 96 726 B → 1096 行 / 71 213 B**（-27%，diff `+302 / -711`）。
- 改法：**删重复 + 压已关闭**，章节编号与引用锚点（§0.6 / §⛔ / §7.0 / §6 P0 等）全部保持不变，
  所有取证结论、偏移、命令、判据逐字保留。
- 主要动作：
  - **新增 §0.2「遗留问题总表」**（L1–L7，按 🔴 挡画面 / ⚠️ 未查清 / ⚪ 收尾 分级）——
    原来散在 §0、§6、§7.0 三处，现在一张表看完。
  - §0.5（第二场）**整节压缩为"已归档结论"**：A 已完成 / B 已作废 / C 已证伪 / D 沿用 / E 三行表；
    删掉 rd9b 的日志摘录与 rd5–rd9b 实测数字表（结论已并入 §7.1-3）。
  - §6 三条 ✅ P0（`AssetLocator.<init>`、`GetReaderWrapper`、`Invalid Reference`）**表格化**，
    只留结论 + 待实现 API 表；5 条"待办方向"指向 §7.0（原文两处重复）。
  - **§12（案例 C）与 §0.6-C/D 是同一次取证的两种写法** → §12 只留「hook 前必读三处汇编」检查清单
    与通用教训，反汇编/args 佐证一律指 §0.6。
  - §10/§11 案例：删过程排查，留判据（假 backtrace 判据、192 B 判据、`native` 变合法相邻地址的指纹）
    与通用教训。
  - §7.1 已完成项（1/2/3/6）压成一行 ✅；§9.3 的 50+ 个 `seq*` 清单压成范围描述。
- 顺手修的两处**风险点**：
  - §9.3 清理命令原写法 `rm -rf .../seq-*` 会**连 `seq-args3` 一起删**（它是 hook 入参证据），
    已改为"先 `ls -d` 预览 + 先 `mv /tmp/keep-seq-args3` 再删"。
  - §11 补回被压缩掉的 native 表项偏移表（`0xe9d008/020/038`），排除法证据完整。
- 回退：文档在 git 里，`git checkout -- docs/HANDOFF-ODDMAR.md` 可还原到第三场版。

### ✅ L1 解决：Oddmar 黑屏 → 画面出来（2026-09-22 晚）

**结论：黑屏解决。** 画面 = Oddmar 标题界面（"ODDMAR" logo + "Press any button to continue"），
X 根窗口截图稳定 **290–300 KB**（判据 ≥180 KB，此前恒 192 B 纯色）。

**真因链**：Unity 把 `jar:file://!/assets/Videos/mobge_and_senri_splash_video.mp4` 交给
`libunity+0x4c124c`（asset 路径翻译，一个虚调用转发 thunk）→ 翻译失败 → `-10004` → 没有片头视频 → 黑屏。

**定稿契约**（消费者函数 `0x53f020`，`bl 4c124c` 在 `0x53f150`；本地副本 build-id `664018de…` 一致）：

| 参数 | 语义 |
|---|---|
| a1 | `string*` **输入/输出**：进来装原始 URL；调用**前**被 `106178(a1, NULL, 0)` 清空；**必须回填本地路径** |
| a2 | `void**` 数据基址；**NULL = 数据在文件里**；非 0 会被加进 offset → 变成传给 extractor 的文件偏移 |
| a3 | `size_t*` 数据总长；同时是 `offset + size <= *a3` 边界检查的上界 |
| 返回 | `bool`（bit0） |

下游：`53f308 open(*a1)` → `[vtbl+136](obj,fd,offset,len)` == `setDataSourceFd`；
或 `53f2c4 [vtbl+144](obj,path)` == `setDataSourcePath`。
外层 `x20 = size`、`x22 = offset`，字面证据是错误串 `"…OpenExtractor offset(%d)+size(%d)"`（`0xbde8d5`）。

**四次迭代（每一步都被日志钉死）**：

| 版本 | 写法 | 结果 |
|---|---|---|
| v1 | 只 `return (uintptr_t)path.c_str()` | bit0==0 → `tbz w0,#0` 恒失败 → `could not translate` → `-10004` |
| v2 | 写 a2=路径指针 / a3=路径长度 + `return 1` | tbz 过了，但 a1 没回填 → `open("")` → `unable to open , error: 0` → `-10004` |
| v3 | 回填 a1 + a2=内存缓冲 / a3=文件长 | 路径与 open 全对（`fd=30`），但 a2 被当 offset 基址 → `offset=70369750858784` → `Invalid data found` → `-10000` |
| **v4** ✅ | 回填 a1 + **a2=NULL** + a3=文件长 + `return 1` | `offset=0`、`tracks=2`、`decoded frame=0/1`、**画面出来** |

**大教训**：`tbz w0,#0` 只能证明"返回值是 bool"，**参数语义必须读完整个消费者函数**
（prologue 到 ret），不能靠"报错消失了"反推 —— v2 走到了 `open`、v3 走到 `open` 成功（`fd=30`），
**两者都还是错的**。

**证据留档**（容器）：`/game/Oddmar/log-seq.txt`（seq-hook4 那轮，81 077 行、`init time`=1、
`ZZZSENTINEL`=0、`Unable to read header`=0）、`seq-hook2/3/4/`、`hook2/3/4.out`。
实现：`projects/unityloader/main.cpp` 的 `bypass_video_translate`。
文档已同步：`docs/HANDOFF-ODDMAR.md` §0.6-C/D/E 更正 + 新增 §0.6-H + §0.1/§0.2/§4.1/§7.0-0a/§8。

**未变**：`BD_BYPASS_VIDEO_TRANSLATE` 仍 opt-in（默认不装）；L2（hostless URL 的 host 来源）仍未查清，
但已不挡画面。另有一条非阻塞性能提示：视频源 1920×1080 > drawable 640×480。

**环境备注**：本轮 Docker Desktop 是关着的（引擎没跑、容器 `Exited(137)`），
是重新启动 Docker Desktop + `docker start GlES_Dev` 恢复的。`docker cp` 用 Git Bash 的 `/tmp` 会被
转成 `D:\tmp`，拷文件回来要用 Windows 风格路径。

### 🔁 L1 复核（同日稍后，只读验证）

回头把 `seq-hook4` 的证据链重跑核对了一遍，结论不变，并修正一处判读规则：

- `log-seq.txt` 实际 **177 423 行**（此前记为 81 077，系笔误）；`ZZZSENTINEL`=0、
  `Unable to read header`=0、`could not translate`=0、`Invalid Reference`=0、`terminate/134`=0。
- `unity.toml` 为正常独立态（`android_package_code="./"` + `android_source_dirs=["./"]`），非哨兵。
- 截图逐帧：前 19 帧 192 B（纯色）→ 1334 → 5874 → 47136 → 92060 → … → **稳定 ~29 万 B**；
  51 帧、总时长 ~60 s。画面：`0030.png`=雪山过场（片头视频在播）、`0050.png`=ODDMAR 标题页
  （"Press any button to continue"）。
- extractor 关键行：`fd source=30 offset=0 length=979746` → `opened tracks=2` →
  `track 0 video/avc 1920x1080` / `track 1 audio/mp4a-latm 48k` → `decoded frame=0/1/2 bytes=3110400`；
  第二遍带 `surface=1` 且 `render=1`（真上屏）。
- **判据修正**：`init time` 读到 **0** 却是成功轮 —— 该值在 `./` 本地目录态下本就为 0，
  **不能单凭它判死**。判活以 `ZZZSENTINEL`=0 + 画面字节数为准。已同步改 `MEMORY.md`
  「容器状态陷阱」一节。

导出的对照截图：`C:/Users/Administrator/AppData/Local/Temp/oddmar-hook4/{f30.png,f50.png}`。

### ✅ L2 定论：hostless URL 的 host 来源已定位（2026-09-22 深夜）

**结论一句话**：host = `Application.dataPath`，它由 `Context.getPackageCodePath()`
（port 侧 `bd_compute_source_dir()`）供给，但被 **Unity 的 `stat()` + `S_IFREG` 闸门**挡下 ——
返回的 `<cwd>/UnityDataAssetPack.apk` **在磁盘上不存在** ⇒ 候选作废 ⇒ `dataPath=""`
⇒ `streamingAssetsPath = "jar:file://" + "" + "!/assets"` ⇒ **host 为空**。

**完整链（静态 + 运行时双向吻合）**

| 环节 | 地址 | 内容 |
|---|---|---|
| URL 构造函数 | `libunity+0x47e940`（唯一调用点 `0x2fd1dc`） | `"jar:file://" + F() + "!/assets"`，**对 F() 无校验** |
| F() | `0x39f800` → `0x4bbe14`（取单例 `*(libunity+0xF0B88)`）→ `0x4bfbe8` | 尾调用 `obj=*(singleton+8)` 的 `vtbl[0x170]` |
| **槽 0x170 真身** | `libunity+0x30c704` | `return std::string(g_appPathBuf)`；`g_appPathBuf` @ `libunity+0xF0B740`（`.bss`），经 GOT 槽 `0xE8BC08`（`R_AARCH64_RELATIVE` addend=`0xf0b740`）取到 |
| **唯一写入者** | `0x39f818`（SetDataPath）← 唯一调用者 `0x31c520` | 包在 `SetDataPathChecked(p)`（`0x31c4cc`）里 |
| **闸门** | `0x31c56c` CheckPath | ① `stat(p)` 必须成功；② `(st_mode & 0xF000) == 0x8000`（**普通文件**）；> 通过才打 `[VFS] Mount %s` |

**运行时铁证**
- `log:660 [BD-DATADIR] getPackageCodePath -> …/UnityDataAssetPack.apk` → `log:661 [NATIVE] stat(同一路径)`：Unity 拿到就 stat。
- 探针：`Application.dataPath = ''`、`streamingAssetsPath = 'jar:file://!/assets'`（Unity 自己就报 hostless）。
- 全仓 `grep -c "\[VFS\] Mount"` = **0**：没有任何候选通过过 stat。
- 运行时 vtbl 与静态推断**逐位一致**：`vtbl=0x3800e5c068`、`vtbl[0x170]=libunity+0x30c704`。

**决定性实验 E1**：`printf ... > /game/Oddmar/gamedata/UnityDataAssetPack.apk` ⇒
`dataPath` 立刻变成 `/game/Oddmar/gamedata/UnityDataAssetPack.apk`（**正是 getPackageCodePath 的返回值**）、
`streamingAssetsPath` 变成合法 jar URL ⇒ 闸门因果坐实。
**但副作用**：启动**直接死**（1372 行、Unity 弹错误对话框），因为 Unity 转去
`stat(<apk>/assets/bin/Data/globalgamemanagers)` 找数据档。**实验后必须删掉该文件**（已删）。

**两条既有说法被更正**
1. §7.0-0b 的"只改 `android_package_code`" A/B **是 no-op**：`bd_compute_source_dir()` 在 staged APK 缺失时
   走 `android_source_dirs` 分支并落到 `logical=<cwd>/UnityDataAssetPack.apk`，与它无关；
   只有新值**以 `.apk` 结尾**才会影响返回值。
2. §0.5-D"探针未接线"作废 —— 已接线（`BD_PROBE_APPPATHS=1`）。

**"根治"评估（当前不建议做）**：需 ①loader 自造最小合法 ZIP 当占位 APK；②扩
`clean_jar_path()`（needle 只有 `"jar:file:/!"` / `"jar:file://!"`，**匹配不到带 host 的绝对形式**，
所以那段 `/assets/` → 磁盘的重写根本不触发）；③`thunks/libc/*` 是跨端口共享代码、要补回归。
收益只是去掉一个 opt-in hook ⇒ 保持现状。

**本场代码改动**（`projects/unityloader/main.cpp`，全部 opt-in/诊断性质）
- `g_probe_app_paths = std::getenv("BD_PROBE_APPPATHS")`；早退条件放宽；解析 `il2cpp_runtime_invoke`。
- `probe_app_paths("post-init")` + `probe_app_paths_pub("video")`（挂在 `bypass_video_translate` 开头，**更可靠**）。
- 新增 `probe_fs_source_pub()`：解 `f0b000+0xB88 → +8 → vtbl[0x170]` 并打印成 `libunity+偏移`。
- 构建：`build-oddmar-verbose`，二进制 mtime 10:15、153 402 768 B；宿主/容器 main.cpp md5 都是 `edd667c8…`。

**产物**（容器 `/game/Oddmar/`）：`log-l2probe.txt`（健康轮 25 MB）、`log-l2apk.txt`（占位轮 89 KB）、
`l2.out`/`l2apk.out`、`seq-l2/`（终态 ~298–301 KB）、`seq-l2apk/`（恒 192 B）；
`/tmp/uni.asm`（96 MB）、`/tmp/relocs.txt`、`/tmp/xref.py` `grab.py` `findg.py`。
**注意**：该 build **vaddr == file offset**，所以 `strings -t x` 的偏移可直接当虚拟地址做 xref。

---

## 2026-09-22 收尾（第五场）

**动作**：文档总结更新 + 代码整理 + 临时文件清理 + 提交（分支 `oddmar`）。

**代码整理**（`projects/unityloader/main.cpp`）
- `g_bypass_video_size` 的日志统一成 `%lld` + `(long long)`（原为 `%d` + `(int)`；值虽放得下但不一致）。
- 该变量补语义注释：`-1` 未探 / `0` 打不开 / `>0` 字节数。
- 重编 `build-oddmar-verbose`：mtime **09-22 11:28**、**153 398 536 B**；缓存核对
  `JNIVM_ENABLE_RETURN_NON_ZERO=OFF` / `BD_ENABLE_LOG=ON` / `BD_ENABLE_VERBOSE=ON` / `RelWithDebInfo` ✅。
  只有一条**既有** warning（`putenv` 字符串常量，`main.cpp:1139`，与本次改动无关）。

**文档更新**（`docs/HANDOFF-ODDMAR.md`，第五版）
- 修正三处过期内容：H1 与文首「`video` 分支」→ **`oddmar` 分支**；§0 更新块的 `ddmar` 旧名加注
  （§0.6 标题/正文同步标注）；**§9.2 判据里「`init time` 必须 >0」改成「不能用来判死活」**（本轮推翻）。
- §0.1 速览补 L2 行；§⛔ 0-4 与 §9.3 新增**清理执行记录**；
  §9.3 数字更正（seq 实为 69 个 / `log*.bak` 实测 5 个，原写 50+ / 7）。
- §0.6、§8 里的二进制时间戳统一为 09-22 11:28。
- §13 反汇编中间件改为「已删 + 两条重生命令」（源 `gamedata/lib/arm64-v8a/libunity.so`，**别删**）。

**清理**（容器，约回收 330 MB）
- `/game/Oddmar/seq*`：**69 → 3**（留 `seq-hook4`=L1 证据、`seq-l2`/`seq-l2apk`=L2 证据），回收 124 MB。
- `/tmp`：`uni.asm`(92M)、`unity.text.asm`(119M)、`relocs.txt`、`xref.py`/`grab.py`/`findg.py`、
  已止损项目的 Skul 历史日志（149M）—— 全删。
- `/game/Oddmar` 根：`log-obb-test*`(6) / `log-hook-test*`(4) / `log-jar-bypass*`(2) 共 12 个过程日志，约 58 MB。
- **有意保留**：`unity.toml.bak-rd9`、`unity.toml.sentinel-0922`、`log*.bak` 5 个、
  `log-args1/2/3.txt` + `args1/2/3.out`、`log-hook4.txt` + `hook4.out`、L2 四件、`log-seq.txt`；
  `/tmp` 的素材 zip 与二进制（`maximus2.zip` 135M 等）**未动**。

**踩坑（值得记）**：`docker cp` 在 Git Bash 里会把主机侧 `/d/Locke/...` 当成 `d:\d\Locke\...`，
报 `CreateFile d:\d: ...` 失败。**主机侧路径必须用 Windows 原生形式**（`D:\...`），
走 PowerShell 工具执行即可；容器侧 `/path` 不受影响。

---

## 2026-09-22 收尾（第六场）：文档改名 + 结论汇总 + 任务整理

**动作**：`docs/HANDOFF-ODDMAR.md` → **`docs/ODDMAR.md`**；补「主要问题：原因/解法」速查；
整理后续任务；提交并 push。

**改名**（`git mv`，Git 识别为 rename）
- 引用同步改到位：`libjnivm/src/jnivm/internal/findclass.cpp:131`、`projects/unityloader/main.cpp:40`、
  `projects/unityloader/javastubs/unity.cpp`（共 3 处，其中简称 `HANDOFF §x` 也写全成 `ODDMAR.md §x`）、
  `docs/ODDMAR.md` 的自引用、`.workbuddy/memory/MEMORY.md`（全部）。
- `.workbuddy/memory/2026-09-2*.md` 是 **append-only 历史日志，不回改**（里面的旧文件名保留原样）。
- 文档内明确标注「本文件原名 `docs/HANDOFF-ODDMAR.md`」，旧引用可对上。

**文档内容**
- 新增 **§0.3「主要问题：症状 → 根因 → 解法」速查表**（7 行：空 `exception_ptr` 导致启动即退出 /
  悬垂 `jmethodID` / 转发 thunk 的 ABI / L1 黑屏 / L2 hostless URL / 启动期两条 NRE 链 /
  NDK 视频符号链），原 §0.3 历史修复顺延为 **§0.4**。
- **§7 改为「后续任务（按优先级）」**，加「剩余待办速览」表，与 §0.2 的 L# 一一对应；
  修掉 §7.0、§9.2 里残留的旧 `init time` 判据；第 8 条清理项标记为已完成。
- 版本行升到**第六版**，`git log --follow` 可追溯改名前的历史。

**关键判断**：动编号前先 grep —— `§0.3` / `§0.4` 在全文的交叉引用计数均为 **0**，所以才敢腾挪。

---

## 资源包瘦身方案（视频 / 贴图）—— 只出方案，未执行

**产出**：`docs/ODDMAR-ASSET-SLIMMING.md`（新）；`docs/ODDMAR.md` 顶部加了指向它的 📌 一行。

**实测口径**（脚本在 `D:/Locke/gitee/LinuxArmPorts/oddmar_port_stage/_analysis/`，未入 git）：
`inventory.py` / `summarize.py`（713 个容器 → 格式直方图）、`texplan.py`（→ texplan.csv）、
`encode_videos.py`（批转视频，全量 18 个约 2.5 min）、`retier_spike.py`（运行时补丁绕开工具缺陷做 ASTC spike）。
主机 Python 3.10（`C:/dev/Python310`）有 UnityPy 1.25.3 + astc_encoder；ffmpeg 8.1 在 `C:/dev/ffmpeg`。

**关键数字**
- 资源：`Videos/` 232 MB（19 个**全 1080p**）、`SoundBanks/`+`Audio/` 158 MB、`bin/Data/` 128 MB（713 文件）、`Bundles/` 38 MB。
- 贴图 **425 张 / ~98 MB**：ETC2_RGBA8 58.0、RGBA32 29.2、Alpha8 字体 8.1；**402/425 带 alpha**；
  **无一张用外部流数据**（`m_StreamData` 全空）。ASTC RGBA 8×8 后 ~41 MB。
- 视频转码实测（全量）：854×480 CRF22 → 149.45 MB（−35%）；**640×360 CRF22 → 115.30 MB（−50%）**；
  640×360 CRF24/500k → 90.79 MB（−61%）。splash 1080p60 → 0.04 MB；`game-start.m4v` 9.88 → 4.68；
  内嵌 `unlockAllLevels`（在 `d8db21c4….resource`，裸 MP4）12.10 → 2.91。
- 基线：`[BD-MEM] hwm=807.8MB`；日志 ~17.7 万行；容器 15.5 GB/32 核（**看不到 OOM，判活只能靠计数+截图**）。

**踩到的两个坑（写进方案，执行前必修）**
1. `tools/unity_astc/astc_retier.py:150` 无脑取 `f.signature` → **裸 SerializedFile 直接 AttributeError**；
   Oddmar 有 191 张贴图在 `bin/Data/<md5>`（裸 SerializedFile）。补丁后实测单文件 8.10 → 0.53 MB。
2. 同文件 `:352` 硬编码 `ASTC_RGB_*` → **丢 alpha**。95% 贴图带 alpha，游戏原厂自带的是 `ASTC_RGBA_4x4`；
   GLES 语义下 RGB 内部格式采样 alpha 恒为 1.0 → 透明变实心。**UnityPy 自己解码看不出来**，只有跑起来才看得见。
   → 必须加 `--alpha-mode auto|rgba|rgb`（RGB/RGBA 块同为 16 B，用 RGBA 不多花钱）。

**另一条前置**：`bypass_video_translate`（`projects/unityloader/main.cpp:124-147`）**把任何 URL 都翻译成写死的 splash 路径**，
`g_bypass_video_size` 只在首调用 stat 一次。所以：① 18 个过场的转码在改 hook 之前无法验收；
② 长度由 `stat` 现取 ⇒ **文件变小不影响契约**（这点是好消息）。修法：按 `a1` 的 URL 逐个映射 + 每路径单独取长度。

**拼上既有文档**：`docs/H700_VIDEO_DECODE_SPIKE.md`（09-17）已定论 **H700 硬解 No-Go**，
且实测 854×480 走同一套 `yuv_gpu` 只有 **23–28 fps**，推荐"离线 854×480@30、低参考帧"。
⇒ 本包 1080p（5.06× 像素）必定解不动；方案里把 `-refs 2 -bframes 2` 并进参数，
分辨率给 640×360（推荐）与 854×480（沿用旧结论）两个档。

**另案（比这两项更大）**：音频 158 MB —— 日志里 `Wwise: Android initialization failure. Reason: AK_Fail`
⇒ 146 MB 的 `.bnk` 可能是纯死重量（实际出声走 fakefmod）；验证成本极低（改名跑一轮）。
另有 `bin/Data/74c32ced…` 28.3 MB 是**单个 MonoBehaviour**；807 MB RSS 峰值仍未归因（建议先量归因再动手）。

---

## 19:45–22:40 Wwise 初始化修复（第二轮，✅ 成功）

**用户指令**：「开始解决 wwise 问题」。

**结论**：`WwiseUnity: Sound engine initialized successfully.` —— **三处根因全在 port 侧**。

### ① `libAkSoundEngine.so` 的 `JNI_OnLoad` 从未被调用

- `JNI_OnLoad`（`0x5c208`）全文只有 5 条指令，唯一作用是 `str x0,[0x430440]`（JavaVM → `.bss`）+
  返回 `JNI_VERSION_1_6`。
- port 用 `so_load` 手动 mmap，只跑 `.init_array`（`loader/so_util.cpp:691` 的 `so_initialize`）。
- ⚠️ `main.cpp` 里 `libmain`(1471) / `libil2cpp`(1490) / `libunity`(1498) / `libBootstrap`(1316)
  **都显式补调了** `JNI_OnLoad`，**唯独 libAkSoundEngine 漏了**。
- 修：加载成功后 `so_symbol(&lak,"JNI_OnLoad")` + `akJNI_OnLoad(&vm, nullptr)`。
- 证据：`[BD-AUDIO] libAkSoundEngine JNI_OnLoad(0x3a0005c208) -> 0x10006 (JavaVM cached)`

### ② `UnityPlayer.currentActivity` 用 `Landroid/app/Activity;` 查不到

- 修完 ① 后 Wwise 前进到「取 Activity」，立刻失败：
  `Unknown Field Getter … Field=currentActivity Signature=Landroid/app/Activity;`
  → `AKDEBUG: Java VM not initialized or not provided in AkInitSettings.`
  → `CRASH: signal 11 (SIGSEGV), fault addr 0x230`
- 根因：jnivm `GetFieldID` **要求 name 与签名同时相等**
  （`libjnivm/src/jnivm/internal/field.cpp:23`），而描述符按 C++ 成员静态类型注册成
  `Lcom/unity3d/player/UnityPlayerActivity;`（`unity.cpp:1162`）。miss 后 jnivm 另造**空 fid**
  （`field.cpp:128-134`）→ getter 返 NULL → Wwise 把 NULL 当 JavaVM 解引用。
- 修：`binding.cpp` 注册 `UnityPlayer` 后，克隆 `currentActivity` 的 `Field`、`type` 改
  `Landroid/app/Activity;`、**共享同一 `get/setnativehandle`**，push 进 `up->fields`。
- ⚠️ **不要把原字段类型直接改成基类** —— 会反过来让按派生类签名查询的调用者失配。**两个签名都要在。**
- 实测打印：`declared signature: Lcom/unity3d/player/UnityPlayerActivity;` → `aliased as 'Landroid/app/Activity;'` ✓

### ③ `AAssetManager_openDir` / `AAssetDir_close` / `AAsset_seek` 有实现没注册

- 修完 ② 后：`Unknown symbol "AAssetManager_openDir"` → `CRASH: signal 6 (SIGABRT)`
- 三者在 `thunks/ndk/asset_manager.c` **都有实现**，但 `thunks/ndk/ndk.cpp` 的 `symtable_ndk`
  只注册了 6 个 AAsset 条目。
- `so_util.cpp:354-372` 的未解析符号是**硬失败**（`reloc_err` → `exit(-1)`），不是延迟降级。
- 修：补 `NO_THUNK` 5 条（含 `AAssetDir_getNextFileName` / `AAssetDir_rewind`）。

### 验收（`build-oddmar` = Release+LOG，60 s，`seq-wwise4`）

| 指标 | 修复前 | 修复后 |
|---|---|---|
| `Sound engine initialized successfully.` | 0 | **1** |
| Awake warning / RenderAudio / PostEvent | 1512 / 1493 / 17 | **0 / 0 / 0** |
| Failed / Android init failure | 1 / 1 | **0 / 0** |
| CRASH / Unknown symbol | 有 | **0 / 0** |
| 四件套 | 0 | **0** |
| 有画面帧 / 最大帧 | — | 29 / 36，231 027 B |
| RSS hwm | 807.8 MB | **663.4 MB** |

### 仍未闭环

1. **没有声音**：`Hardware audio subsystem stopped responding. Silent mode is enabled.`（60 s 内 6 次）。
   Wwise 的输出后端要 **OpenSL ES**，而 `thunks/opensles/opensles.cpp` **默认关闭**
   （`CMakeLists.txt:275`，关掉是为了让 libunity 自己的 FMOD 走
   `org.fmod.FMODAudioDevice` → `SDL_QueueAudio`）。开 shim 会**同时改变 FMOD 的选路**，
   要实测；更稳的是在 `dlsym_impl` 里**按调用者模块分派**。
2. **147 MB `SoundBanks/` 是否死重仍未验证**：60 s 内 `LoadBank`=0、`.bnk` 引用=0、RSS 未涨
   ⇒ 标题界面阶段根本不碰 bank（运行时按需加载）⇒ **必须进关卡才能测**。

### 踩到的坑

- 有一次跑测日志只有 **646 行**、卡在 `[BD-EGL_SDL] before window`，连 Wwise 都没到 ——
  **同一二进制复跑即恢复**，是已知的启动竞态（与之前 130 s 那轮同类）。判活前先确认日志量级。
- 用 `grep NO_THUNK\("[^"]+"` 提取 port 符号时**漏了字符串拼接式**（如
  `NO_THUNK("SL_IID_" #name, …)`）—— 算符号差集时要留意，否则会误报缺失。

### 可复用手法

**`libAkSoundEngine.so` 的 UND 符号 − port 的 `NO_THUNK` 注册集合 = 完整差集**，
一次算全，避免一轮一轮试。本次 96 条差集里只有 3 个是「Android 特有且 host 没有」。

### 改动文件

- `projects/unityloader/main.cpp`（+ libAkSoundEngine JNI_OnLoad 调用）
- `projects/unityloader/javastubs/binding.cpp`（+ `currentActivity` 的 Activity 签名别名）
- `thunks/ndk/ndk.cpp`（+ 5 条 AAsset 注册）
- `docs/ODDMAR.md`（新增 §0.8）、`docs/ODDMAR-ASSET-SLIMMING.md`（§5 重写）

---

## 第三轮收官（23:0x–23:45）：Wwise **出声**打通 + 一条"假故障"教训

### 结论

**Oddmar 标题界面现在既出画面也出 PCM。** 验收轮 `SEQ_DIR=/game/Oddmar/seq-audio-verify`、
`SECS=75`、`build-oddmar`：判活四件套 + SEGV/terminate/Unknown symbol/GL error/NRE **全 0**，
截图 **274 665 B = 标题界面**，`Silent mode` **0 次**、`Enqueue` **218** 次、`queued` 在
12 544–15 616 B 间波动（设备在消费）。

### 三处根因（详见 `docs/ODDMAR.md` §0.9）

1. **sink 工厂优先选 AAudio，而它被 port 的哨兵骗了**。`libAkSoundEngine.so` 的
   `0x1f39e8 → 0x2278f8`（按 `SDK_INT>26` 选后端），探测函数 `0x226d4c` 实体就是
   `return (dlopen("libaaudio.so") != NULL)`；`dlopen_impl` 对**未注册库返回 `0xDEAD`（非 NULL）**
   ⇒ 误判 AAudio 可用 ⇒ 27 个 `AAudioStream_*` dlsym 全 miss ⇒ 静音。
   **修**：`libaaudio.so` → `NULL`。Silent mode 11 → 0。
2. **shim 缺 `SL_IID_BUFFERQUEUE`**（Wwise 的 UND 符号之一，与
   `SL_IID_ANDROIDSIMPLEBUFFERQUEUE` 共用同一 bufQ VTbl）。**修**：补分支 +
   `bd_iid_name()` 诊断。`unsupported` 156 → 0。
3. **SDL 2.0.10 单输出设备**：同进程第二次 `SDL_OpenAudioDevice` 必失败
   （default / by-name 都一样 —— **写了最小 C 程序实测**，所以放弃"按设备名重试"）。
   **修**：新增 `platform/common/audio_bus.{h,cpp}`（音频总线仲裁），Wwise 复用 FMOD 的设备，
   FMOD 侧发现 owner≠FMOD 就 `sleep 5ms; continue;` 让路；设备 24 kHz ⇒ 加
   `bd_resample_s16()` 做 48k→24k。另 `dlsym_impl` 改**按调用者模块分派**
   （`bd_is_opensl_symbol()` + `bd_caller_is_ak_sound_engine()`，靠新增的
   `so_module_containing(__builtin_return_address(0))`），**只放行 `libAkSoundEngine`**，
   否则 libunity 自己的 FMOD 会改选 OpenSL 卡住。

### 🔴 本轮最大的教训：一个"假故障"吃掉了一整轮排查

15:32–15:43 连续 **11 轮全黑**，连**改动前遗留的旧二进制**（`build-oddmar-verbose`，11:28）也全黑，
差点判定"环境漂移、与改动无关"。**真因：跑测少了 `BD_BYPASS_VIDEO_TRANSLATE=1`。**
那条画面修复是 **opt-in**（文档里明写"默认不装"），漏了就**完整复刻修复前的黑屏**。

排除过程（都做了，值得记）：
- 容器 GL/X 栈**健康** —— 写了个最小 SDL2+GLES2 清屏探针（`/tmp/bdgles.c`），同一 Xvfb 上
  正常出帧（`GL_RENDERER = llvmpipe (LLVM 12.0.0, 128 bits)`，根窗口截图 209 B / RGB=255,38,13）。
- X 窗口**存在** —— `xwininfo -root -tree` 有 `0x20000e "Teapot"`（WM_CLASS `unityloader`）
  640×480+0+0，但**根窗口与子窗口单独截图都是 192 B** ⇒ 不是合成问题，是没东西可合成。
- `gamedata/` 自 **12:53** 起未变，而 13:03–14:39 多轮有画面 ⇒ 资源排除。
- 旧的 `seq-*` 目录对照表（最大 PNG 字节数）一眼看出黑/有画面的分布：
  `seq-final`(13:10)=282 795 / `seq-wwise4`(14:39)=231 027 / `seq-ab-*`(15:32)=192。

**判据定了：有画面轮 `[BD-VIDEO] redirect external bind` = 3，黑屏轮 = 0。**
⇒ 黑屏第一步就 `grep` 这个计数，别再折腾显示号/Xvfb/旧二进制。

**一句话教训：「换了没变化」≠「改动无关」** —— 变量可能根本不在被测对象里。

### 改动文件（`oddmar` 分支，尚未提交）

- `thunks/libc/misc.cpp`（`dlopen_impl` AAudio→NULL；`dlsym_impl` 按调用者分派 OpenSL）
- `loader/so_util.{h,cpp}`（+ `so_module_containing()`）
- `thunks/opensles/opensles.cpp`（+ `SL_IID_BUFFERQUEUE`、共享 SDL 设备、重采样、诊断、逃逸开关）
- `platform/common/audio_bus.{h,cpp}`（**新增**，音频总线仲裁）
- `projects/unityloader/javastubs/fakefmod.cpp`（publish + 让路）
- `CMakeLists.txt`（`BD_ENABLE_OPENSLES_SHIM` 默认 ON）
- `docs/ODDMAR.md`（新增 §0.9；§0.1/§0.3/§0.8/§1.3/§1.4 更新）
